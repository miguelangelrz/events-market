export type MarketSelection = {
  id: string;
  name: string;
  price: number;
};

export type UserMarketSelection = {
  id: string;
  name: string;
  price: number;
  eventId: string;
  eventName: string;
  marketId: string;
  marketName: string;
}
